#!/usr/bin/env bash
# Coil TaskCompleted hook
# Placeholder for utility boosting logic.
# In practice, the agent calls coil_feedback(id, true) explicitly during
# the session for memories it found useful. This hook is reserved for
# future batch processing if needed.

set -euo pipefail

exit 0
